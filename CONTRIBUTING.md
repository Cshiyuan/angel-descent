# 贡献指南

感谢你考虑为"天使下凡一百层"项目做出贡献！

## 🎯 贡献方式

我们欢迎以下类型的贡献：

- 🐛 Bug 修复
- ✨ 新功能开发
- 📝 文档改进
- 🎨 UI/UX 优化
- ⚡ 性能优化
- 🌐 国际化翻译

## 📋 开始之前

1. **搜索现有 Issue**：查看是否已有相关问题或功能请求
2. **创建 Issue**：如果没有，请先创建 Issue 讨论你的想法
3. **等待反馈**：在开始大量工作前，等待维护者的反馈

## 🔧 开发流程

### 1. Fork 项目

点击右上角的 "Fork" 按钮，将项目 fork 到你的账号下。

### 2. 克隆项目

```bash
git clone https://github.com/YOUR_USERNAME/angel-descent.git
cd angel-descent
```

### 3. 创建分支

```bash
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

分支命名规范：
- `feature/xxx` - 新功能
- `fix/xxx` - Bug 修复
- `docs/xxx` - 文档更新
- `refactor/xxx` - 代码重构
- `perf/xxx` - 性能优化

### 4. 配置开发环境

1. 复制配置文件：
```bash
cd hello-weminigame
cp project.config.json.example project.config.json
```

2. 编辑 `project.config.json`，填入你的微信小游戏 AppID

3. 使用微信开发者工具打开项目

### 5. 进行开发

- 遵循项目的代码风格（见下文）
- 编写清晰的注释（中文）
- 确保代码在微信开发者工具中能正常运行

### 6. 测试你的更改

- 在模拟器中测试
- 在真机上测试（推荐）
- 确保没有引入新的 Bug
- 测试不同屏幕尺寸的适配

### 7. 提交代码

```bash
git add .
git commit -m "feat: 添加新功能描述"
```

提交信息规范（使用中文）：
- `feat: 新功能描述`
- `fix: Bug 修复描述`
- `docs: 文档更新描述`
- `refactor: 重构描述`
- `perf: 性能优化描述`
- `style: 代码格式调整`
- `test: 测试相关`

### 8. 推送到远程

```bash
git push origin feature/your-feature-name
```

### 9. 创建 Pull Request

1. 在 GitHub 上打开你的 fork
2. 点击 "New Pull Request"
3. 填写 PR 描述：
   - 说明你做了什么
   - 为什么要这样做
   - 如何测试
   - 相关的 Issue 编号

## 📝 代码风格

### JavaScript 规范

```javascript
// ✅ 好的命名
const playerSpeed = 200;
const isGameOver = false;

// ❌ 不好的命名
const s = 200;
const flag = false;

// ✅ 使用 ES6+ 语法
const player = new Player();
import { GAME_STATES } from './constants.js';

// ✅ 清晰的注释（中文）
/**
 * 玩家类
 * @description 处理玩家移动、碰撞和状态
 */
export default class Player extends Sprite {
  // ...
}

// ✅ 函数命名清晰
updatePlayerPosition(deltaTime) {
  // 实现
}

// ❌ 函数命名模糊
update() {
  // 实现
}
```

### 文件组织

- 每个文件一个类
- 使用 ES6 模块（import/export）
- 文件名使用 kebab-case（如 `player-manager.js`）
- 类名使用 PascalCase（如 `PlayerManager`）

### 注释规范

```javascript
// ✅ 好的注释
/**
 * 计算玩家与平台的碰撞
 * @param {Player} player - 玩家对象
 * @param {Platform} platform - 平台对象
 * @returns {boolean} 是否发生碰撞
 */
function checkCollision(player, platform) {
  // 实现
}

// ❌ 不必要的注释
// 循环数组
for (let i = 0; i < arr.length; i++) {
  // ...
}
```

## 🚫 注意事项

### 必须避免

1. **不要使用浏览器 API**
   - ❌ `document.querySelector()`
   - ❌ `window.addEventListener()`
   - ✅ 使用微信小游戏 API（`wx.*`）

2. **不要在帧更新函数中打印日志**
   - ❌ 在 `gameStep()` 中使用 `console.log()`
   - ✅ 使用条件调试或采样输出

3. **不要使用 Canvas 阴影效果**
   - ❌ `ctx.shadowBlur`
   - ✅ 使用渐变或多层绘制代替

4. **不要直接修改配置文件**
   - ❌ 修改 `project.config.json` 中的 appid
   - ✅ 仅修改 `.example` 模板文件

### 性能要求

- 保持 60fps 流畅运行
- 避免内存泄漏
- 使用对象池管理游戏对象
- 及时清理不再使用的资源

## 📚 开发文档

在开发前，请务必阅读：

- [CLAUDE.md](hello-weminigame/CLAUDE.md) - 开发指南和注意事项
- [GAME_DESIGN_DOCUMENT.md](hello-weminigame/GAME_DESIGN_DOCUMENT.md) - 游戏设计文档
- [WEMINGAME.md](hello-weminigame/WEMINGAME.md) - 微信小游戏特性

## 🐛 报告 Bug

### Bug Report 模板

```markdown
**Bug 描述**
清晰简洁地描述 Bug

**复现步骤**
1. 进入 '...'
2. 点击 '....'
3. 滚动到 '....'
4. 出现错误

**期望行为**
描述你期望发生什么

**实际行为**
描述实际发生了什么

**截图**
如果适用，添加截图帮助说明问题

**环境信息**
- 设备：[例如 iPhone 12]
- 系统：[例如 iOS 15.0]
- 微信版本：[例如 8.0.28]

**额外信息**
添加其他关于问题的上下文信息
```

## ✨ 功能请求

### Feature Request 模板

```markdown
**功能描述**
清晰简洁地描述你想要的功能

**问题背景**
描述这个功能要解决什么问题

**建议的解决方案**
描述你期望的实现方式

**替代方案**
描述你考虑过的其他解决方案

**额外信息**
添加其他关于功能请求的上下文或截图
```

## 📮 Pull Request 流程

1. **PR 标题**：使用清晰的标题，如 "feat: 添加排行榜功能"
2. **PR 描述**：详细说明你的更改
3. **关联 Issue**：在描述中引用相关 Issue（如 `Fixes #123`）
4. **代码审查**：耐心等待维护者审查
5. **修改反馈**：根据反馈进行必要的修改
6. **合并**：通过审查后，维护者会合并你的 PR

## 🎖️ 成为贡献者

完成首次贡献后，你将：
- 被添加到贡献者列表
- 获得项目徽章
- 在更新日志中被提及

## 💬 社区准则

- 尊重他人
- 保持友善和专业
- 接受建设性批评
- 关注对项目最有利的事情

## ❓ 需要帮助？

如果你有任何问题：
- 查看 [Issues](https://github.com/Cshiyuan/angel-descent/issues)
- 阅读项目文档
- 创建新的 Issue 提问

---

再次感谢你的贡献！🎉
